# システム設計書：TwilightPad MOD Generator

> **現在の実装との関係**: 本書は `twilight_pad.py`（単体スクリプト時代）を対象にした原初の設計書であり、
> §3〜4 の音響工学的分析・音楽理論的解決策（サンプル音源の狙い、コード進行、手続き型作曲の考え方）は
> 現在も nostalgic ジャンルの設計根拠として有効。ただし実装は以後2段階で移設されている:
> `mod_weaver/profiles/nostalgic.py`＋`nostalgic_samples.py`（[EXTENSION_DESIGN.md](EXTENSION_DESIGN.md) Phase 1b）、
> さらにサンプル合成本体は `core/synth.py` の Patch 方式へ（[EXTENSION_DESIGN.md](EXTENSION_DESIGN.md) D15、
> [CORE_EXTENSION_DESIGN.md](CORE_EXTENSION_DESIGN.md) §8）。§3.2 の合成式は数式としては現行実装と同一値を保っている。

## 1. 概要

本ドキュメントは、Python標準ライブラリのみを用いてProTracker形式の4チャンネル・トラッカー音楽ファイル（`.mod`）をプログラマティックに合成・生成するシステム「**TwilightPad MOD Generator** (`twilight_pad.py`)」の設計仕様書である。
特に、初期生成スクリプト（Copilot生成コード）において発生していた「ノスタルジックさが感じられない」という課題に対する**改修の観点・音響工学的分析・音楽理論的解決策**、および「**ノスタルジックな美学を保ったまま、実行するたびに無限に異なる名曲を生成する手続き型（プロシージャル）作曲エンジン**」の設計仕様を詳述する。

---

## 2. 課題分析と改修の観点

初期実装コードでは、以下の2点において致命的な技術的・音楽的欠陥が存在し、ノスタルジックな楽曲としての体をなしていなかった。

### 2.1. サンプル音源設計の課題と改修観点

```
【初期コードの問題】
sr = 44100 で生成
      │
      ▼ (ProTracker再生クロック: 約8287Hz)
再生速度が約5.3倍間延び、音程が2オクターブ以上沈下
      │
      ▼
「超低周波のうなり・ノイズ」と化す
```

1. **サンプリングレートの不整合（致命的欠陥）**:
   - 初期コードは `sr = 44100 Hz` で波形データを生成していた。
   - しかし、Amiga ProTracker（PALクロック 3,546,895 Hz）において、基準ノート（C-3, Period 214）で再生されるサンプリング周波数は **約 8,287.14 Hz** である。
   - この結果、生成された波形はトラッカー再生時に **約5.3倍間延び** し、ピッチも極端に低下し、意図したリズム・音程を全く保持できていなかった。
   - **【改修観点】**: サンプルの生成サンプリングレートを Amiga 再生レートに完全一致する `SR = PAL_AMIGA_CLOCK / (214.0 * 2.0) ≈ 8287.14 Hz` と定義し、数学的にピッチと時間が1:1で正確に再生されるよう基底設計を刷新。

2. **Pad（持続音）のループ処理の欠落**:
   - 初期コードの Pad はサンプル長がわずか 512 サンプルしかなく、かつヘッダー設定が `loop_length = 1`（ループなし）であった。
   - このため、再生時に約 0.06 秒で音が途切れ、「プチッ」というクリックノイズとともに無音化していた。
   - **【改修観点】**: 1,024 サンプルの中に整数周期（$K=32$ 周期など）が過不足なく収まる数学的合成アルゴリズムを導入。始端と終端の位相を完全一致させ、ヘッダーで `loop_length = 512 words` を指定することで、ノイズ皆無の**完全シームレス・ループ持続音**を実現。

3. **打楽器・効果音の合成破綻**:
   - 初期コードのスネアは `sin(800t) + sin(1200t)` という固定の正弦波を鳴らしており、ホワイトノイズを含まないため「ピピッ」という不快な電子ビープ音になっていた。
   - また、ハイハット等の高域パーカッションが存在せず、リズムの骨格と空気感が欠落していた。
   - **【改修観点】**: 指数減衰ピッチベンドを持つ **Lo-Fi Warm Kick**、トーンとローパスフィルタ処理ノイズを混成した **Soft Snare**、差分ハイパスと金属共鳴を付加した **Closed Hi-Hat** を新規設計。

4. **ノスタルジーを想起させる音色の不在**:
   - 初期コードは歪んだノコギリ波ベースとパッドのみで、メロディを担う楽器が一切存在しなかった。
   - **【改修観点】**: 郷愁・夕暮れの切なさを象徴する澄んだ高次倍音を持つ **Music Box（オルゴール／トイチャイム）**、低域を温かく包む **Warm Bass**、素朴な木管リード **Mellow Flute** を導入。

---

### 2.2. パターン・楽曲構成設計の課題と改修観点

1. **音程テーブル（Period Table）の狂い（致命的欠陥）**:
   - 初期コードの `PERIODS` 定義において、`"D#3": 381`（実際はD音）、`"G-3": 303`（実際はF#音）が誤登録されていた。
   - これにより、コード構成音が C - D - F# という増4度（トライトーン）を含む不協和音となり、不気味な不快音を発生させていた。
   - **【改修観点】**: ProTracker PAL標準の **3オクターブ・36音（C-1 〜 B-3）を完全にカバーする正確なPeriodテーブル** を構築。

2. **和声理論の欠如と全行トリガーによるノイズ化**:
   - 初期コードはコード進行が存在せず、単一のコード構成音が 64 行すべての行で毎行トリガーされていた。
   - これにより、毎行アタックのクリップノイズが機関銃のように乱打される「バグノイズ」と化していた。
   - **【改修観点】**: 適切な休符と音符長を配置。日本人やレトロゲーム音楽ファンの心を捉える **ノスタルジック下降進行（夕暮れの王道）** と **ii-V-I-vi 進行** を導入。

3. **楽曲構成（ドラマ性）の欠落**:
   - 初期コードはわずか 64 行（約10秒）の単一パターンのみで、イントロもサビも展開も存在しなかった。
   - **【改修観点】**: **Intro（静寂とオルゴール） $\to$ Theme A（Lo-Fiビート合流） $\to$ Theme B（想い出のサビ展開） $\to$ Theme A $\to$ Outro（余韻とフェードアウト）** の全5シーケンス構成を設計。

4. **テンポ・音量表現（ダイナミクス）**:
   - 初期コードはテンポ指定がなくデフォルトの BPM 125 でせわしなく再生されていた。
   - **【改修観点】**: 落ち着いた夕暮れを想起させる **BPM 88〜96（seed ごとに 88/90/92/94/96 から選択）** に設定。ProTrackerのボリューム設定コマンド（`0x0C`）をセル単位で制御し、強拍/弱拍、ハイハットのゴーストノート、アウトロのフェードアウトをプログラミング。

---

## 3. 音響工学的合成アルゴリズム仕様

全サンプルは 8-bit signed PCM（-128 〜 127）として合成される。

### 3.1. サンプル一覧

| ID | サンプル名 | 長さ (bytes) | 基準ピッチ | ループ設定 | 音色解説 |
|:---|:---|:---|:---|:---|:---|
| 1 | `LoFiKick` | 1,658 | C-3 | ループなし | 46Hzへ指数降下するピッチベンドを持つ丸いキック |
| 2 | `SoftSnare` | 1,492 | C-3 | ループなし | 175Hz胴鳴り＋ローパス処理ノイズのレトロスネア |
| 3 | `ClosedHH` | 372 | C-3 | ループなし | 金属共鳴（3.2kHz/4.4kHz）＋ハイパス減衰ハット |
| 4 | `WarmBass` | 5,966 | C-3 (C4相当) | ループなし | サイン基音＋第2倍音＋アタック緩和のふくよかなベース |
| 5 | `MusicBox` | 9,530 | C-3 (C4相当) | ループなし | 基音＋2/3倍音＋非整数5.4倍音＋自然指数減衰オルゴール |
| 6 | `TwilightPad` | 1,024 | C-3 (C4相当) | 完全ループ (0..512w) | 整数周期（K=32,16,48）による温かいアナログシンセパッド |
| 7 | `MellowFlute` | 1,024 | C-3 (C4相当) | 完全ループ (0..512w) | 奇数倍音主体の素朴で切ない木管風リード |

### 3.2. 合成アルゴリズム詳説

#### (1) Lo-Fi Warm Kick
- 瞬時周波数の指数関数的推移: $f(t) = 46 + 64 \cdot e^{-30t}$
- 位相関数積分: $\phi(t) = 2\pi \int_0^t f(\tau) d\tau = 2\pi \left( 46t + \frac{64}{30}(1 - e^{-30t}) \right)$
- 振幅エンベロープ: $A(t) = e^{-11.5t}$
- ソフトサチュレーション: $v(t) = \tanh(1.3 \cdot \sin(\phi(t)) \cdot A(t))$

#### (2) Nostalgic Soft Snare
- 胴鳴り成分: $T(t) = \sin(2\pi \cdot 175 \cdot t) \cdot e^{-22t}$
- スナッピー（ノイズ）成分: 擬似乱数 $N_{raw} \in [-1, 1]$ を 1次IIRローパスフィルタ $LP_i = 0.35 LP_{i-1} + 0.65 N_{raw}$ に通し、$S(t) = LP(t) \cdot e^{-14t}$
- 合成波形: $v(t) = \tanh(1.25 \cdot (0.45 T(t) + 0.55 S(t)))$

#### (3) Music Box (オルゴール)
- 澄んだ金属棒の共振モデル:
  $$v(t) = \text{Attack}(t) \cdot \tanh \left( 1.1 \cdot \sum_{k \in \{1, 2, 3, 5.4\}} w_k \sin(2\pi \cdot k \cdot f_0 \cdot t) e^{-\alpha_k t} \right)$$
  - 基音 $f_0 = 261.63$ Hz: $w_1 = 0.55, \alpha_1 = 2.0$
  - 第2倍音 $2f_0$: $w_2 = 0.25, \alpha_2 = 3.5$
  - 第3倍音 $3f_0$: $w_3 = 0.12, \alpha_3 = 5.0$
  - チャイム特有の非整数倍音 $5.4f_0$: $w_4 = 0.08, \alpha_4 = 12.0$

#### (4) Twilight Ambient Pad (完全整数周期ループ設計)
- サンプル長 $L = 1024$ サンプル。
- ループ内における基本波の波数 $K = 32$（実効周波数 $f \approx 259$ Hz）。
- 各高調波の周期がすべて $L$ の約数となるよう設計:
  $$v(i) = \tanh \left( 0.95 \cdot \left( 0.55 \sin\left(\frac{2\pi \cdot 32 i}{L}\right) + 0.30 \sin\left(\frac{2\pi \cdot 16 i}{L}\right) + 0.22 \sin\left(\frac{2\pi \cdot 64 i}{L}\right) + 0.08 \sin\left(\frac{2\pi \cdot 96 i}{L}\right) \right) \right)$$
- $i=0$ と $i=1024$ で波形値および傾きが完全にゼロクロス連続するため、ループ境界のクリックノイズが物理的に存在しない。

---

## 4. 音楽理論・楽曲構成設計

### 4.1. 調性とコード進行

- **Key**: C Major / A Minor（白鍵中心、郷愁を呼ぶモーダルインターチェンジ）
- **Tempo**: BPM 88〜96（seed ごとに選択。ゆったりとした家路のリズム、Speed 6）
- **1パターン**: 64行（16行 × 4小節、約10.4秒）

#### テーマA: ノスタルジック下降進行（夕暮れの情景）
| 小節 | 行範囲 | コード | 構成音 | 感情の動態 |
|:---|:---|:---|:---|:---|
| Bar 1 | Rows 0..15 | **Fmaj7** | F - A - C - E | 茜色に染まる空、切なさと広がり（長7度の浮遊感） |
| Bar 2 | Rows 16..31 | **Em7** | E - G - B - D | 静かに暮れゆく光、穏やかな哀愁 |
| Bar 3 | Rows 32..47 | **Dm7** | D - F - A - C | 街の灯り、過ぎ去った日々への郷愁 |
| Bar 4 | Rows 48..63 | **Cmaj7** | C - E - G - B | 暖かな我が家、安心感と儚い余韻 |

#### テーマB: 想い出のサビ展開（エモーショナルな回想）
| 小節 | 行範囲 | コード | 構成音 | 感情の動態 |
|:---|:---|:---|:---|:---|
| Bar 1 | Rows 0..15 | **Dm7** | D - F - A - C | 語りかけるような導入 |
| Bar 2 | Rows 16..31 | **G7** | G - B - D - F | 高まる想い、見上げる空 |
| Bar 3 | Rows 32..47 | **Cmaj7** | C - E - G - B | 溢れ出す温かい記憶（高音メロディ） |
| Bar 4 | Rows 48..63 | **Am7** | A - C - E - G | 切なく胸を締め付ける余韻 |

---

### 4.2. 4チャンネルのステレオ配置設計

Amigaハードウェアのパンニング固定仕様（Ch1: Left, Ch2: Right, Ch3: Right, Ch4: Left）を活かした配置：

```
     【Left Channel】                       【Right Channel】
   ┌──────────────────┐                   ┌──────────────────┐
   │ Channel 1: Drums │                   │ Channel 2: Bass  │
   │ (Kick/Snare/HH)  │                   │ (Warm Mellow)    │
   └──────────────────┘                   └──────────────────┘
   ┌──────────────────┐                   ┌──────────────────┐
   │ Channel 4: Melody│                   │ Channel 3: Pad   │
   │ (MusicBox/Lead)  │                   │ (Ambient Strings)│
   └──────────────────┘                   └──────────────────┘
```

- 左チャンネルにリズムの芯とキラキラした主旋律を配置。
- 右チャンネルに包み込む低域ベースと広がりのある持続和音パッドを配置。
- ヘッドフォンやステレオスピーカーで聴いた際、中央〜左右に自然な空間的広がりが生まれる。

---

### 4.3. 手続き型（プロシージャル）自動作曲エンジン設計

ノスタルジーを崩壊させずに「実行するたびに異なる音楽」を無限に生成するため、**制約付き手続き型生成（Constrained Procedural Generation）** を採用している。

```
                       【乱数シード (Seed)】
                               │
       ┌───────────────────────┼───────────────────────┐
       ▼                       ▼                       ▼
【コード進行の選択】     【動機 (Motif) 生成】      【ビート・ダイナミクス】
 5つのノスタルジック     リズム型と音程輪郭の       Lo-Fiドラムのゴースト、
 プリセットから選択      反復・変奏 (A-A'-B-Cadence) フィルイン、微小テンポ変動
       │                       │                       │
       └───────────────────────┼───────────────────────┘
                               ▼
              【ProTracker パターンシーケンス構築】
```

1. **コード進行プールの確率的選択**:
   - `Step-Down (夕暮れ下降進行)`: `Fmaj7 -> Em7 -> Dm7 -> Cmaj7`
   - `Royal Road (王道進行)`: `Fmaj7 -> G7 -> Em7 -> Am7`
   - `Saudade (サウダーデ進行)`: `Dm7 -> G7 -> Cmaj7 -> Am7`
   - `Journey (回想・旅立ち進行)`: `Am7 -> Fmaj7 -> Cmaj7 -> G7`
   - `Canon Sunset (カノン夕景)`: `Cmaj7 -> G7 -> Am7 -> Em7`
   Theme A と Theme B に異なるプリセットを割り当てることで、豊かな楽曲展開を担保。

2. **動機（Motif）と対位法ルールに基づくメロディ生成**:
   - **音価（リズム骨格）**: 歌心のあるリズムテンプレートから動機を選択し、小節1と小節2で同じ動機を反復（変奏）させて「覚えやすい旋律」を形成。
   - **強拍（拍頭）の選択**: コードトーン（特に切ない長7度、3度、5度）を優先配置。
   - **経過音のステップ進行**: 前の音から75%の確率で順次進行（1〜2度差）、25%の確率で跳躍進行を行い、跳躍後は反対方向へステップバックする（ポピュラー音楽の黄金律）。
   - **終止（Cadence）**: フレーズの末尾は解決音に着地。
   - **サビ（Theme B）**: オクターブを高音域へシフトし、感情の昂ぶりを演出。

3. **完全な再現性（Seed Reproducibility）**:
   - 実行時に `Seed`（乱数シード）を決定・表示。`--seed <番号>` を指定することで、過去に生成した気に入った曲をビット完全（MD5ハッシュ一致）で100%再現可能。

---

## 5. MODバイナリ構造仕様

生成されるファイルは標準的な **ProTracker 4-channel ("M.K.") MOD** 形式に厳密に準拠する。

1. **Song Title**: 20 bytes (`b"Twilight Pad"`, null-padded)
2. **Sample Headers**: 31 samples × 30 bytes = 930 bytes
   - サンプル長、ループ開始位置、ループ長はすべて 16-bit word（Big Endian）で記録。
   - ループ無効時は `loop_start = 0, loop_length = 1`。
3. **Song Positions**: 1 byte (`5`)
4. **Restart Position**: 1 byte (`0x7F`)
5. **Pattern Order Table**: 128 bytes (`[0, 1, 2, 1, 3] + [0]*123`)
6. **Magic Tag**: 4 bytes (`b"M.K."`)
7. **Pattern Data**: 4 patterns × 1024 bytes = 4,096 bytes
   - 1セル = 4 bytes（Note Period, Sample Number, Effect, Parameter）
8. **Sample Data**: 各サンプルの 8-bit signed PCM データを連続配置（総計 21,116 bytes）
- **総ファイルサイズ**: 26,246 bytes

---

## 6. 検証と品質保証

1. **構造整合性テスト**:
   - `mod_weaver/core/verify.py`（独立実装のパーサ＋構造検査 V01〜V16）により、全ヘッダーフィールドのワード長、オフセット、パターンサイズ、総ファイルサイズがバイト単位で一致することを検証する（旧サンプル `old/TwilightPad.mod` は 26,246 bytes）。
2. **再生互換性**:
   - OpenMPT、XMPlay、VLC、および各種Amigaエミュレータにて正常にロード・再生可能であることを確認。
